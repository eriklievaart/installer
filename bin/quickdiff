#!/bin/sh

set -e

die() {
    echo >&2 "$@"
    exit 1
}

[ "$#" -eq 2 ] || die "2 arguments required: [DIR A] [DIR B]"
[ -d "$1" ] || die "not a directory: $1"
[ -d "$2" ] || die "not a directory: $2"

for sub in $(ls $1); do
	echo $(du -d 0 $1/$sub)
done

